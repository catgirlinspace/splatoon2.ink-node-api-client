'use strict';

/**
 * Fetches API data
 * @param {number} number
 * @param {string} locale
 * @return {string}
 */
module.exports.getCurrentStages = function(mode) {
    if (mode) {
      return;
    }else{
      return data;
    }
};
